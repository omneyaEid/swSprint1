package swproject;
public abstract class update extends NotificationImp
{
    public update(String language, String subject, int userID, int notificationID) {
        super(language, subject, userID, notificationID);
    }
}
